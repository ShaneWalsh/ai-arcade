import { CanvasContainer } from "./CanvasContainer";
import { UiSettings } from "./UiSettings";

export class DrawingContext {
  constructor(public cc:CanvasContainer, public uiSet:UiSettings) {
  }
  // may want methods later
}